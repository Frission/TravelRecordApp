<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TravelRecordApp.App">
    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="ColorDarkGreen">#1F8B00</Color>
            <Color x:Key="ColorGhostWhite">#F8F8FF</Color>
            
            <!--if a style has no key, it'll be applied to all targets, implicit style-->
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource ColorDarkGreen}"/>
                <Setter Property="TextColor" Value="{StaticResource ColorGhostWhite}"/>
            </Style>

            <Style TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor" Value="{StaticResource ColorDarkGreen}" />
                <Setter Property="BarTextColor" Value="{StaticResource ColorGhostWhite}"/>
            </Style>

            <Style TargetType="TabbedPage">
                <Setter Property="BarBackgroundColor" Value="{StaticResource ColorDarkGreen}" />
                <Setter Property="BarTextColor" Value="{StaticResource ColorGhostWhite}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>